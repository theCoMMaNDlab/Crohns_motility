# Python dependencies for the standalone post-processing and DOE scripts.
#
#   pip install -r requirements.txt
#
# Covers the scripts run with a normal Python 3.8+ interpreter:
#   motility_metric.py   (numpy)
#   generate_doe.py      (standard library only)
#   run_doe.py           (standard library only)
#
# NOT covered: export_triad_fields.py and motility_from_cross_sections.py
# import odbAccess and abaqusConstants, which ship inside Abaqus and are not
# installable from PyPI.  Run those two with the Abaqus interpreter instead:
#   abaqus python export_triad_fields.py --odb m1.odb --out triad_fields.inp
#   abaqus viewer noGUI=motility_from_cross_sections.py

numpy>=1.20
